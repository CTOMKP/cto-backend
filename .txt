//to establish connection with contabo vps
ssh root@84.54.23.80


// incase of firewall
# Allow PostgreSQL default port
sudo ufw allow 5432/tcp

# Allow SSH (just to be safe)
sudo ufw allow 22/tcp

# Enable the firewall if it's not already enabled
sudo ufw enable

# Check the firewall status
sudo ufw status

sudo systemctl restart postgresql
sudo systemctl status postgresql

Confirm pg_hba.conf allows all IPs

Run this on your VPS:

HBA="$(sudo -u postgres psql -tA -c "SHOW hba_file;")"
echo "host    cto_db    cto_admin    0.0.0.0/0    md5" | sudo tee -a "$HBA"